version: "3.9"
services:
  api:
    build: ./backend
    environment:
      - FRONTEND_ORIGIN=http://localhost:3000
    ports: ["8000:8000"]
  web:
    build: ./frontend
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
    ports: ["3000:3000"]
    depends_on: [api]
